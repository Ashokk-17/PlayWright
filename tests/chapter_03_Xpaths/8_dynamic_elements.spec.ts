import { test, expect, Locator } from "@playwright/test";

test("Verify the dynamic", async ({ page }) => {

    await page.goto("https://testautomationpracticeblogspot.com/");

 await page.waitForTimeout(5000);










});